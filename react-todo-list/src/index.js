import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

/* ROOT ELEMENT */
const baseElement = document.getElementById('root');

/* RENDER */
ReactDOM.render(
    <App />,
    baseElement
);